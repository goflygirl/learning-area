
        const existingText = document.querySelector('#message');
        console.log(existingText);

        const button = document.querySelector('#btn');
        console.log(button);

        //Event Handler for the button
      btn.addEventListener('click', () => {
        alert('The button was clicked!');
      });